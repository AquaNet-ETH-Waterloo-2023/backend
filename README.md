## Development

### Quickstart

This creates a `venv` folder and installs all the app dependencies, then immediately runs a dev server.

```bash
make quickstart
```

### Run dev server

```bash
make dev
```
